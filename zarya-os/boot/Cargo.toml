# Bootloader for Zarya OS
# UEFI application that switches to long mode and transfers control to kernel

[package]
name = "zarya-boot"
version = "0.1.0"
edition = "2021"

[dependencies]
uefi = "0.24"
x86_64 = "0.14"

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
lto = true
